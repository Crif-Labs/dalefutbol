import { OrdenarHorariosPipe } from './ordenar-horarios.pipe';

describe('OrdenarHorariosPipe', () => {
  it('create an instance', () => {
    const pipe = new OrdenarHorariosPipe();
    expect(pipe).toBeTruthy();
  });
});
